import type { source } from "./Utils/Resources";

const groupeFolderName = "2-arts-graphiques";

const sources: source[] = [
  {
    name: "painting",
    path: `/${groupeFolderName}/models/painting.gltf`,
    type: "gltfModel",
  },
];

export default sources;
